{
  "name": "event-loop",
  "author": "Charly Gomez",
  "license": "MIT",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for Event Loop - Cloudflare Workers API + Pages Web",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "deploy": "pnpm run deploy:api && pnpm run deploy:web",
    "deploy:api": "pnpm --filter=@event-loop/api deploy",
    "deploy:web": "pnpm --filter=@event-loop/web pages:deploy:production",
    "deploy:preview": "pnpm run deploy:api:preview && pnpm run deploy:web:preview",
    "deploy:api:preview": "pnpm --filter=@event-loop/api deploy",
    "deploy:web:preview": "pnpm --filter=@event-loop/web pages:deploy"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}